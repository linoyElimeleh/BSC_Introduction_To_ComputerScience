#include <stdio.h>
#include <stdbool.h>

void main()
{
	int numberForTest = checkLen(333);
	int checkDateValue = checkDate(8, 4, 1998);
	int checkIsOpen = IsOpen(1,13);
	int numberIsPerfect = isPerfect(496);
	int checkIfContains = isContained(2265123, 651);

}